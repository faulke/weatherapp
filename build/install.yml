# must specify platform
platform: linux

# docker image with node pre-installed
image_resource:
  type: docker-image
  source:
    repository: node
    tag: 'latest'

inputs:
- name: weather
  path: .

outputs:
- name: weather-npm
  path: ./weather

run:
  path: sh
  args: ["weather/build/install.sh"]
