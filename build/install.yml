# must specify platform
platform: linux

# docker image with node pre-installed
image_resource:
  type: docker-image
  source:
    repository: node
    tag: 'latest'

inputs:
- name: weather

outputs:
- name: weather
  path: .

run:
  path: sh
  args: ["weather/build/install.sh"]
