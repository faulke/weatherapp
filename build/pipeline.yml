resources:
- name: weather
  type: git
  source:
    uri: https://github.com/faulke/weatherapp.git
    branch: concourse-pipeline

jobs:
- name: run-tests
  plan:
  - get: weather
  - task: npm install
    file: weather/build/install.yml
  - task: run tests
    file: weather/build/tests.yml
